export default function Private() {
  return (
    <h1>
      To create folder which would be excluding from routing system use
      underscore before folder Name . example: _private, _lib
    </h1>
  );
}
